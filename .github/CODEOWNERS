# Who owns what in this repo.
#
# LAST matching line wins, not the first.
#
# ⚠️ THIS REPOSITORY IS PUBLIC AND RUNS IN THE CUSTOMER'S OWN GCP PROJECT, as the customer,
# from their Cloud Shell. It is the one repository where a bad merge executes somewhere we
# do not control and cannot roll back. Everything here is customer-facing by construction.
#
# ⚠️ ADVISORY, NOT ENFORCING. CODEOWNERS only blocks a merge when branch protection sets
# `require_code_owner_reviews` and requires an approving review. This repository has no
# branch protection today, so these lines auto-request a reviewer and nothing more.
#
# ⚠️ An owner without write access is silently ignored.
#
# Keep this file free of customer identifiers, project ids and service-account emails, in
# line with the rest of the repository.

* @dmiiq @IgniteIQ-Ryan
